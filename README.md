
# ProofBox — Starter (Vite + React + TS + Tailwind)

Rendu identique à la maquette (mode sombre inclus), prêt à lancer.

## Démarrage
```bash
npm install
npm run dev
```

Ouvre l'URL indiquée (http://localhost:5173).
